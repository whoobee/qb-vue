<template>
  <div class="wrapper">
    <side-bar>
      <template slot="links">
        <sidebar-link to="/dashboard" :name="$t('sidebar.dashboard')" icon="tim-icons icon-components"/>
        <sidebar-link to="/navigation" :name="$t('sidebar.navigation')" icon="tim-icons icon-map-big"/>
        <sidebar-link to="/teleop" :name="$t('sidebar.teleop')" icon="tim-icons icon-controller"/>
        <sidebar-link to="/debug" :name="$t('sidebar.debug')" icon="tim-icons icon-settings"/>
        <sidebar-link to="/config" :name="$t('sidebar.config')" icon="tim-icons icon-settings-gear-63"/>
      </template>
    </side-bar>
    <div class="main-panel">

      <top-navbar></top-navbar>

      <dashboard-content @click.native="toggleSidebar">

      </dashboard-content>

      <content-footer></content-footer>
    </div>
  </div>
</template>
<style lang="scss">
</style>
<script>
import TopNavbar from "./TopNavbar.vue";
import ContentFooter from "./ContentFooter.vue";
import DashboardContent from "./Content.vue";
import MobileMenu from "./MobileMenu";
export default {
  components: {
    TopNavbar,
    ContentFooter,
    DashboardContent,
    MobileMenu
  },
  methods: {
    toggleSidebar() {
      if (this.$sidebar.showSidebar) {
        this.$sidebar.displaySidebar(false);
      }
    }
  }
};
</script>
